'use strict'

const User = exports = module.exports = {}
// const NotificationRepository = make('App/Repositories/NotificationRepository')

User.created = async (user) => {
  //
}

User.updated = async (user) => {
  //
}

User.deleted = async (user) => {
  //
}

User.registered = async (user) => {
//   await NotificationRepository.create(user.id, user.agent_id, {
// 		action: `user.registered`,
// 		text: `notification.user.registered`,
// 		link: `/users/member`,
// 		// parameters: { title: message.title }
// 	})
}
